FROM ghcr.io/mlflow/mlflow:v2.15.1

# psycopg2-binary: driver PostgreSQL (não incluído na imagem base do MLflow)
# boto3: cliente S3/MinIO para o artifact store
RUN pip install --no-cache-dir psycopg2-binary boto3
