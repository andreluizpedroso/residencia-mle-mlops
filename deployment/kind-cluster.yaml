kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: fraud-detection
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30800   # API fraud-detection
        hostPort: 8080
        protocol: TCP
