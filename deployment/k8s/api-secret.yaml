apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  namespace: fraud-detection
type: Opaque
# Em produção: usar Vault, Sealed Secrets ou External Secrets Operator.
# Para dev local, stringData é suficiente.
stringData:
  AWS_ACCESS_KEY_ID: "minioadmin"
  AWS_SECRET_ACCESS_KEY: "minioadmin_secret"
